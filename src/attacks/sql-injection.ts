import type { AttackDefinition } from '../types';
import { AttacksMeta } from '../data/attacks';

const meta = AttacksMeta.find((a) => a.slug === 'sql-injection')!;

export const sqlInjection: AttackDefinition = {
  meta,
  appliesIf: [
    { head: 'DBに接続している:', body: 'PostgreSQL/MySQL/SQLite等のRDBに対しSQLを発行する全アプリが対象。NoSQLでも同種のクエリ注入は起こりうる。' },
    { head: '検索/フィルタ/ソート機能:', body: 'ユーザ入力を WHERE / LIKE / ORDER BY 列名・方向に反映する処理は要警戒。' },
    { head: 'パスパラメータの数値ID参照:', body: '/users/:id のような数値想定パラメータを文字列のままSQLに連結している。' },
    { head: 'ログインフォーム:', body: 'WHERE email=… AND password=… を文字列連結で組み立てている実装。' },
    { head: '管理者向け一覧/CSVエクスポート:', body: '社内ツールでも文字列連結でSQLを書く運用は同様にリスク。' }
  ],
  caseStudy:
    '2011年 ソニーの関連サービスでSQLインジェクションにより数千万件規模の利用者情報が流出。2015年 大手通販サイトでも同種の事案で個人情報・カード情報が窃取された。',
  damage: [
    { head: '情報漏洩:', body: '会員情報・カード情報・社内機密などをDBから一括で抜かれる。' },
    { head: '改ざん/削除:', body: '価格・権限・記事などを書き換えられ、業務停止に直結。' },
    { head: '認証回避:', body: "「' OR 1=1 --」のような構文で管理画面にログインされる。" },
    { head: '横展開:', body: 'DB権限を起点にOSコマンド実行や他サーバ侵入へ繋がる。' }
  ],
  defense: [
    { head: 'プレースホルダ/バインド変数:', body: '文字列連結でSQLを組み立てない。最も確実な対策。' },
    { head: '最小権限のDBユーザ:', body: 'アプリ用DBユーザに DROP/GRANT などの権限を与えない。' },
    { head: 'WAF導入:', body: '既知の攻撃パターンを多層防御で遮断。' },
    { head: 'エラー詳細の非表示:', body: 'SQLエラーを画面/ログに露出させない。' },
    { head: '入力値の型チェック:', body: '数値項目は数値であることをアプリ層で検証。' }
  ],
  devNote: [
    { head: 'ORM任せにしない:', body: '生SQLを書くAPIや LIKE / ORDER BY 列名のような可変箇所は注意。' },
    { head: 'IDの型を意識:', body: 'パスパラメータの数値IDは Number(strict) で受ける。' },
    { head: 'マイグレーション/管理スクリプト:', body: '本番DBに対する直接実行ツールも対象。社内向けでも例外にしない。' },
    { head: 'ログ出力時のサニタイズ:', body: '入力値をそのままログに書くと2次インジェクションが起きうる。' },
    { head: '静的解析/テスト:', body: 'sqlmap風テスト・lint・依存ライブラリのCVE監視をCIに組み込む。' }
  ],
  setup(stage) {
    stage.addGroup({ id: 'server-side', x: 0.36, y: 0.18, w: 0.60, h: 0.66, label: 'サーバサイド', variant: 'infra' });
    stage.addActor('attacker', { pict: 'attacker', pos: { x: 0.10, y: 0.5 }, label: '攻撃者' });
    stage.addActor('app',      { pict: 'server',   pos: { x: 0.50, y: 0.5 }, label: 'Webアプリ' });
    stage.addActor('db',       { pict: 'database', pos: { x: 0.88, y: 0.5 }, label: 'DB' });
    stage.addConnection('app', 'db', { id: 'app-db', variant: 'flow', label: 'SQL' });
  },
  steps: [
    {
      title: '攻撃ペイロードの投入',
      description: "攻撃者が入力欄に「' OR 1=1 --」のようなSQL断片を含む文字列を投入する。",
      run: (stage) => {
        stage.sendPacket('attacker', 'app', { duration: 1100, payload: "' OR 1=1 --" });
      }
    },
    {
      title: 'SQL文が組み立てられDBへ送信',
      description: 'アプリが入力値を文字列連結でSQLに埋め込み、意図しないSELECT文がDBへ送信される。',
      run: (stage) => {
        stage.sendPacket('app', 'db', { duration: 1100, payload: 'SELECT * WHERE id=… OR 1=1' });
      }
    },
    {
      title: 'DBが全件を返却',
      description: '"OR 1=1" により条件が常に真となり、DBがテーブル全行を返却してしまう。',
      run: (stage) => {
        stage.flashActor('db', 'shake', 1000);
        stage.sendPacket('db', 'attacker', {
          duration: 1300,
          className: 'benign',
          payload: '全テーブルの行を返却'
        });
      }
    },
    {
      title: '情報漏洩成立',
      description: '本来見せるべきでないデータが攻撃者の手元に渡る。バインド変数を使えば防げた典型例。',
      run: (stage) => {
        stage.flashActor('attacker', 'shake', 800);
      }
    }
  ],
  beginner: {
    summary:
      '入力欄に「パスワード」のつもりで命令文を書き込まれて、データベース(会員情報の一覧表のようなもの)から全部のデータを抜かれてしまう攻撃。',
    appliesIf: [
      { head: 'データを保存するDBを使っている:', body: '会員情報・商品・記事などをデータベースに保存しているなら、ほぼ全アプリが対象です。' },
      { head: '検索欄や絞り込み機能がある:', body: '入力された文字をそのままDB問い合わせに混ぜている可能性があるので要注意。' },
      { head: 'URLに数字のIDが入っている:', body: '`/users/123` のようなURL。数字以外の文字でも処理が通ってしまう作りだと危険。' },
      { head: 'ログイン機能がある:', body: 'メールとパスワードを照合する処理で文字を直接くっつけてSQLを作っているなら要注意。' },
      { head: '管理者向けの一覧画面やCSV出力がある:', body: '社内向けでも、文字をくっつけてSQLを書いていれば同じ罠。' }
    ],
    caseStudy:
      '2011年に有名なゲーム関連サービスで、入力欄に細工をされて何千万件もの会員情報(名前・メールアドレスなど)が一気に流出する事件がありました。2015年には大手通販サイトでも、同じ手口でクレジットカード情報を含む個人情報が盗まれています。',
    damage: [
      {
        head: '情報がごっそり盗まれる:',
        body: '会員情報・カード番号・社内の秘密情報などが、データベース(データの保管庫)から一気に持ち出されます。'
      },
      {
        head: 'データを書き換えられる/消される:',
        body: '商品の値段・記事の内容・利用者の権限などを勝手に書き換えられ、サービスが止まったり混乱したりします。'
      },
      {
        head: 'ログイン画面をすり抜けられる:',
        body: '「\' OR 1=1 --」のような呪文を入力されると、パスワードを知らなくても管理者としてログインされてしまうことがあります。'
      },
      {
        head: '他のシステムにも侵入される:',
        body: 'データベースを足がかりに、サーバ自体や他のサービスにも入り込まれます。'
      }
    ],
    defense: [
      {
        head: 'プレースホルダ(?マーク)を使う:',
        body: 'データベースへの命令文(SQL)を、プログラム内で文字を直接くっつけて作るのをやめます。代わりに「ここに値が入る」という穴(?マーク)だけを書いて、値は別ルートで渡す書き方を使えば、入力された文字が命令文として解釈されません。一番確実な対策です。'
      },
      {
        head: 'データベース利用者の権限を絞る:',
        body: 'アプリが使うデータベースのアカウントには、表(テーブル)を消したり強い権限を渡したりできる機能を最初から与えないようにします。'
      },
      {
        head: '攻撃を弾く番人ソフト(WAF)を入れる:',
        body: 'Webサーバの前に「怪しいリクエストを自動で弾く監視ソフト」を置いて、二重三重に守ります。'
      },
      {
        head: 'エラー画面の中身を見せない:',
        body: 'エラー画面にデータベース内部の情報が出ると、攻撃者にヒントを与えてしまいます。利用者に見せるのは「エラーが起きました」程度に。'
      },
      {
        head: '数字の項目は数字か確認する:',
        body: '「年齢」のように本来数字しか来ない項目に文字列が来たら拒否する、というチェックをアプリ側でも必ずやります。'
      }
    ],
    devNote: [
      {
        head: 'ライブラリ任せにしない:',
        body: 'ORM(SQLを楽に書ける道具)を使っていても、自分でSQLを書く部分や、検索条件に列名を渡す箇所は要注意です。'
      },
      {
        head: 'URLの数字IDは厳密にチェック:',
        body: 'URLに含まれる数字(例: /users/123)は、本当に数値に変換できる文字列かを必ず確認してから使いましょう。'
      },
      {
        head: '管理ツールやメンテ用のスクリプトも対象:',
        body: '社内向けや運用向けのツールでも、SQLを文字列でくっつけて作っている箇所は同じ危険があります。例外にしないこと。'
      },
      {
        head: 'ログに入力値をそのまま書かない:',
        body: 'ログにユーザの入力をそのまま書き込むと、ログを処理する別のシステムで再びインジェクションが起きることがあります。'
      },
      {
        head: '自動チェックをCIに入れる:',
        body: 'GitHub Actions などの「コードを自動で検査する仕組み(CI)」で、コードの問題を見つける道具(静的解析)や、使っているライブラリの脆弱性検知を毎回回しましょう。'
      }
    ],
    steps: [
      {
        title: '攻撃者が変な文字列を入力する',
        description:
          '攻撃者がフォーム(検索欄やログイン欄)に、ふつうの値ではなく「\' OR 1=1 --」という、データベースへの命令文の一部になってしまう文字を入力します。'
      },
      {
        title: 'アプリがその文字列を命令文に組み込んでデータベースへ送る',
        description:
          'アプリが入力された文字をそのままくっつけて命令文を作るので、本来意図しない「全部見せて」という命令ができあがり、データベースへ送られます。'
      },
      {
        title: 'データベースが全部のデータを返してしまう',
        description:
          '「OR 1=1」は「常に正しい」という意味の条件式です。これがあると条件チェックが効かず、データベースは表(テーブル)の中身を全部返してしまいます。'
      },
      {
        title: '情報漏洩が起きてしまった',
        description:
          '本来見せてはいけないデータが攻撃者の手に渡ります。最初の一歩で「プレースホルダ(?マーク)」という安全な書き方をしていれば、これは起きませんでした。'
      }
    ],
    actorLabels: {
      attacker: '攻撃者',
      app: 'アプリ(画面の裏で動くプログラム)',
      db: 'データベース(データの保管庫)'
    },
    groupLabels: {
      'server-side': 'サーバの中(画面の裏側)'
    },
    connectionLabels: {
      'app-db': 'データベースに命令を送る'
    }
  }
};
